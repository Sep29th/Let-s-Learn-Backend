import { ResponseApiFormatInterceptor } from './response-api-format.interceptor';

describe('ResponseApiFormatInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseApiFormatInterceptor()).toBeDefined();
  });
});
